"use client";

/**
 * Deliberately empty.
 *
 * This used to drift glowing embers across a near-black page. On a light warm
 * background that reads as smudges, and the whole point of the repaint was to
 * calm things down — the photos and the letter should be what draws the eye.
 *
 * Kept as a component rather than deleted because every page imports it, and
 * because it's the obvious place to put something back if the pages ever feel
 * too bare.
 */
export function BackgroundEffects() {
  return null;
}
